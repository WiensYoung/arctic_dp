# Ice model sensitivity config
profile: paper
seeds: 10
controllers:
  - full
scenarios:
  include_groups: [G]
output:
  root: results/sci1_submission
  save_traces: true
  save_figures: true
runtime:
  fail_fast: false
  skip_missing_optional_controllers: true
